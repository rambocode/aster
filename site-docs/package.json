{
  "name": "aster-docs-site",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:gen": "node scripts/split-help.mjs",
    "docs:dev": "npm run docs:gen && vitepress dev",
    "docs:build": "npm run docs:gen && vitepress build",
    "docs:preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
