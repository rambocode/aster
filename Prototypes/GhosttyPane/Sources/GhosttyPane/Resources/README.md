# Generated Ghostty resources

Run `scripts/setup-ghostty-poc.sh` from the repository root. The script stages
`ghostty/` and its sibling `terminfo/` here from the same locked Ghostty revision
used to build `GhosttyKit.xcframework`.
