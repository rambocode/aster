# P0 实施证据

状态：P0 已通过；最终核销见文末「P0 综合审计通过」。以下按执行顺序保留历史记录，早期未完成状态不代表当前状态。工作区：`.worktrees/codex-remote-work`，分支：`codex/remote-work`，基线 commit：`9e33e7b`。未提交、未发布。

## 2026-09-08

- Zig 实际版本为 0.15.2。
- 新增独立 `SessionRuntime/build.zig`、版本入口和有界增量分帧解码器。
- `zig build test` 通过 4 个行为用例：单字节分片、合并帧边界、超长头预分配拒绝与错误连接隔离、EOF 截断。
- `zig build` 成功；`zig build -Dtarget=x86_64-linux-musl --prefix .build/linux` 成功。这仅验证骨架构建，不证明 PTY 或 VT 能力。
- Ghostty 源码已检出固定 revision，`src/build/GhosttyLibVt.zig` 和 `build.zig` 确认存在独立 libghostty-vt 构建入口。
- VT host 构建成功，新增独立 C ABI 适配层；`zig build test-vt -Dvt-prefix=.build/vt-host` 通过两个测试，验证跨块中文 UTF-8 与宽字符光标推进、非法尺寸拒绝。
- 原 master 基线非并行 Swift 测试已结束，日志 `/tmp/aster-remote-baseline.log`。工具报告 exit 0，但日志包含 Agent 生命周期及界面断言失败且没有完整结束汇总，因此基线不能判为通过；进程查询确认没有残留测试宿主。该结果需继续诊断，不能用退出码掩盖失败。
- Linux VT 构建首次遇到依赖下载 `TlsInitializationFailed`，重试成功。
- 新增跨平台 PTY 启动层，子进程失败报告、5 秒启动超时、明确进程/FD 所有权及失败回收。macOS PTY 两项测试通过。
- 通过 SSH 向 OrbStack 隔离目录 `/root/.local/state/aster-test/p0-20260908-runtime` 上传并执行 Linux PTY 两项、VT 两项测试，全部通过。未安装系统服务。
- 有界 active-screen formatter 往返测试发现上游 cursor 在 tabstop 设置后偏移（3→72），新增可重放补丁修复输出顺序；修复后 macOS 与 Linux 的 active-screen 往返测试通过。
- 可复现 headless VT 构建入口为 `SessionRuntime/scripts/build-vt.sh`，固定版本、检测意外源码修改并重放补丁。

剩余：VT 编译/适配、PTY/bridge 链路、完整协议操作 schema 与 Swift fixtures、A01–A03 真实验收。不能将当前结果标成任何完整 A 用例通过。

## 本轮验证结果

- `zig build test test-pty test-vt -Dvt-prefix=.build/vt-host --summary all`：9/9 构建步骤成功，12/12 测试通过（协议 4、PTY 2、VT 6）。
- `scripts/build-vt.sh x86_64-linux-musl` 和 Linux `test-binaries` 构建成功；通过 SSH 实际执行 PTY 2/2、VT 6/6 通过。
- VT 新增有界格式化、颜色/覆盖内容/光标往返、服务端 cursor query 唯一应答及应答队列溢出检测。画面不含原始查询，因此新客户端不会重复回应。
- 补丁严格应用到固定 Ghostty revision；构建脚本拒绝覆盖额外本地源码修改。未修改既有 App XCFramework。
- `bash -n SessionRuntime/scripts/build-vt.sh` 与 `git diff --check` 通过。

下一步：组合 PTY 与 VT 的服务事件循环、桥接协议及完整快照。当前 `formatActiveScreen` 不代表已恢复非活动屏幕/图形，也未完成 Aster App 输入/画面接入。P0、A01–A03 仍未通过完整验收。

## PTY / VT / socket / bridge 链路

- 实现 `Session` 单进程事件循环：有界读取、输入队列、查询应答回写、真实 resize、退出回收；生命周期独立于客户端。
- 实现私有 Unix socket 的 `probe-serve`：校验目录属主/权限和 peer UID，socket 0600，断开连接保留进程，显式 terminate 回收。只宣告 `p0_active_screen`。
- 实现 `probe-bridge`：输入使用 base64 保留跨读取 UTF-8 字节边界，画面经过有界分帧解码，支持正常 raw-mode 恢复和前缀分离。
- macOS `test-session` 10/10 通过（包含 2 个新集成用例与 8 个底层用例）。Linux 交叉构建后在 OrbStack 运行同样 10/10 通过。
- macOS 和 OrbStack 的 `tests/probe.py` 均通过：3 次附加/分离 PID 不变；逐字节中文经 bridge 到 Shell 并显示回复；前缀分离保活；显式终止后 PID 不存在、socket 清理。
- Linux 产物只上传至已有隔离测试目录；未安装系统服务，测试进程由用例创建并回收。

未完成边界仍包括完整协议 schema/Swift fixtures、非活动屏幕/图形完整快照、App/Ghostty 实际窗口输入与画面验证，以及正式服务状态/租约。probe 的正常前缀分离测试不证明异常信号和手机终端恢复。P0 仍进行中。

## Swift / Zig 分帧与握手互操作

- 新增 AsterCore `SessionWireFrame` / `SessionWireDecoder`，每次最多消费一帧，正确处理 Data 的非零切片下标，长度在 payload 分配前校验；错误连接不能继续解码。
- 新增 `SessionHandshake` 与 Zig `handshake.zig`，明确主版本和必需能力门槛，未知可选能力/字段不破坏兼容性。
- 两端读取同一 `framing-fixtures.json` 和 `hello-fixtures.json`。分帧逐个拆分位置测试控制/二进制帧、非法长度/类型、截断；握手覆盖兼容扩展、不兼容主版本、缺能力、重复/非法能力和非法身份。
- `zig build test --summary all`：7/7 测试通过。
- `./scripts/test.sh --filter 'remoteFrame|remoteHandshake'`：实际 SwiftPM 项目 7/7 测试通过，日志 `/tmp/aster-remote-wire-tests.log`。此前单独 frame 3/3 也已通过。
- worktree 仅以忽略的符号链接复用原工作区已生成 Ghostty Framework/资源；Swift 构建缓存位于本 worktree，没有修改 master 源码。
- `hello.schema.json` JSON 语法与 `git diff --check` 通过。

握手类型目前尚未替换 probe 入口，不能据此宣称正式会话连接已实现。其他操作 schema、完整屏幕/图形快照、App 真实界面验收仍未完成，A02 与 P0 不标记完成。

## 握手进入真实桥接链路

- probe 服务使用共享 Hello 结构发送主次版本、规范 UUID 身份、平台与 `p0_active_screen`。身份在 probe 进程内稳定，不把它描述为已经实现的持久服务身份。
- bridge 在握手格式、主版本及 probe 所需能力验证成功前，不轮询 stdin、不切 raw mode、不渲染画面；5 秒内没有完成握手返回明确超时。
- macOS / OrbStack 真实子进程拒绝测试覆盖错误主版本、缺少能力、非法身份、先发画面四种情况：预置输入没有发出，stdout 没有渲染内容。
- 已有中文分片、前缀分离、相同 PID 重连及显式结束链路回归通过。
- Linux 重连测试首次发现 PID 文件存在但未写完的夹具竞态；改为临时文件写完后原子重命名，随后重跑通过。
- Zig 协议与握手测试 7/7 通过。生产必需能力集合保持不变；probe 使用明确的独立能力要求，不冒充生产服务。

下一步仍是完整屏幕状态与 App 真实接入；尚未完成完整操作 schema、持久服务身份、生产租约或 P0 全部验收。

## 主/备用屏幕导出

- 新增独立 headless 扩展 `ghostty_aster_screen_format_buf`，读取指定屏幕不改变活跃屏幕；没有初始化的备用屏幕返回缺失状态。补丁记录于 `0002-screen-export.patch`。
- `Terminal.formatScreen` 在分配前测量/限制输出，`activeScreen` 查询当前屏幕；没有改动应用侧 Framework ABI。
- 构建脚本支持完整两补丁集，核对固定 revision 与全部 tracked diff，拒绝覆盖额外源码改动。
- 新测试覆盖读取非活动主屏幕不改变备用屏幕/光标、未初始化备用屏幕保持缺失、两屏幕重建及 1049 切换还原。
- 首次测试错误地假定备用屏幕从第 0 列开始，且使用不保存光标的 47 切换；实际 VT 证据显示进入时继承光标。测试改为核对导出前后状态不变，并使用 1049 保存/还原验证主屏幕光标。
- 最终 macOS `test-vt` 9/9、OrbStack Linux `vt-tests` 9/9 通过。Linux 首轮执行旧测试产物失败，重新构建上传后通过，未将失败结果计入通过证据。

完整快照仍需整合全局输入模式、保存的光标状态、图形缓存及线协议分块，当前服务仍只发送 active-screen probe 帧；P0 未完成。

## 快照分块与应用门禁

- 新增有界快照组装器，声明长度上限 32 MiB，分块顺序和 SHA-256 必须一致后才返回可应用的载荷。
- 服务发送 begin/索引分块/end，桥接端按事务组装后一次性交给终端；首份快照完成前不读取 stdin。连接 EOF 留有半份事务时失败。
- 加入首份/在途快照 5 秒超时；原来握手后的无限等待不再存在。
- Zig 协议测试 11/11 通过。macOS/OrbStack 五项缺块、错序、摘要错误、超限、重叠故障注入通过，stdout 无半份画面，输入未提前发出；原有中文/重连测试通过。
- 额外增加保持连接但不完成快照的超时用例，执行结果见后续记录。

事务目前承载 active-screen probe 载荷，尚未将双屏幕/模式/图形组装成完整快照，也未完成生产 revision/epoch 路由；P0 仍未完成。
- macOS 六项故障注入（含保持连接的超时）最终全部通过；Linux 本轮验证五项，新增超时用例尚未在 Linux 重跑。

## 桥接尺寸与异常退出

- Linux 补跑六项快照故障注入（含超时），全部通过。
- 新增桥接专属 self-pipe 信号唤醒，SIGWINCH 同步实际 TTY 尺寸，SIGTERM/SIGHUP/SIGINT 经正常退出路径恢复终端；没有以周期轮询代替尺寸事件。
- 握手完成后读取并发送初始 TTY 尺寸，服务端 resize 后安排新画面。未完成握手时不向 socket 发尺寸或输入。
- `tests/bridge_pty.py` 使用真实宿主 PTY 验证 33×101 初始尺寸、41×123 动态尺寸、SIGTERM 后原终端配置恢复、受管子进程仍存活，再显式回收测试任务。
- macOS 首次严格比较 termios 发现仅 `PENDIN`（536870912）变化；该字段为返回 canonical 模式时的内核输入重处理状态。测试保留所有实际配置比较，仅排除该瞬时标志。
- 最终 macOS 与 OrbStack 的桥接 PTY 测试均通过。此证据不等价于 App 实际窗口或手机 SSH 验收，P0 仍未完成。

## 真实 AppKit / Ghostty 窗口集成

- 新增 opt-in `RemoteBridgeWindowTests`，仅在显式提供 `ASTER_SESSION_PROBE_BINARY` 时运行，不让常规测试隐式下载/安装运行时。
- 测试创建隔离 AppKit 窗口和真实 Ghostty surface，执行 bridge 子进程；验证 READY、中文输入/回复，销毁 surface 后 PID 存活，新 surface 附加后显示原输出并能继续输入。
- 使用项目要求的 `./scripts/test.sh --no-parallel --filter remoteBridgeRendersAndReattachesInGhosttyWindow`，最终 1/1 通过，日志 `/tmp/aster-remote-window-tests.log`，最后运行耗时约 0.536 秒。
- 测试仅清理所拥有的服务进程和 surface；服务结束关闭 PTY，不按可能已被回收复用的 child PID 发送结束信号。
- 新增 `SessionRuntime/scripts/test-window.sh`，构建固定 VT/运行时并显式启用该测试；脚本语法检查通过。

这是真实 AppKit/Ghostty 组件集成，但输入由测试调用标准终端输入接口，不替代用户级菜单/键盘/截图的完整 UI 验收。当前画面仍是 probe active-screen 载荷，完整快照、操作 schema 和 P0 总体验收未完成。

## 双屏幕进入真实快照数据路径

- `display_snapshot.capture` 生成同时包含主/备用文本屏幕的重建序列，probe 实际传输该序列，bridge 只在事务完整性确认后应用；不再只传活动屏幕。
- 往返用例覆盖 active alternate 重连后退出到原主屏幕、active primary 重连后仍能进入保留的备用屏幕。
- 模式查询验证括号粘贴、基础鼠标、SGR 鼠标和 Kitty 键盘标志在重建前后一致。
- 新增 `snapshot_transaction_v1` 能力门槛，旧 raw-frame probe 在握手时被拒绝；负向测试检查具体错误名，防止快照测试因提前握手拒绝而误通过。
- macOS `test-session` 17/17 通过；OrbStack 最新 `session-tests` 17/17 通过，中文重连、六项快照故障、五项握手拒绝及真实 PTY 尺寸/清理回归全部通过。
- 当前运行时再次通过实际 AppKit/Ghostty 窗口集成 1/1（约 0.572 秒）；日志 `/tmp/aster-remote-window-tests.log`。

图形缓存、完整保存的光标/模式栈、origin/scroll-region 复杂组合、生产操作 schema 及最终 UI 验收仍未完成；P0 不标记通过。

## 原点模式与滚动区域坐标

- 新增往返验证复现真实偏移：原光标查询为 `ESC[2;9R`，恢复后为 `ESC[6;9R`，文本内容相同但坐标错误。
- 修正固定 formatter 补丁：终端级恢复在 origin mode 下使用滚动区域相对 CUP 坐标，独立屏幕导出仍保持绝对坐标；保留先恢复 margins/tabstops 再定位光标的顺序。
- 同一用例覆盖纵向滚动区域、横向边距与 origin mode 组合；比较完整文本和终端真实查询回复，不仅检查字符串存在。
- macOS 与 OrbStack 最新 `test-session` / `session-tests` 均为 18/18 通过。首次编译的匿名结构推导错误已修正，失败结果未计为通过。
- host 运行时已重新链接新 VT 补丁；中文重连及真实 PTY 桥接回归执行结果见后续。

完整保存光标/模式栈、图形缓存、生产操作 schema 和最终 UI 验收仍未完成。上述两种区域组合通过不代表所有控制序列组合已穷尽；P0 仍进行中。
- 新 host 运行时的中文分片/重连保活和真实 PTY 尺寸/SIGTERM 清理回归均通过。

## 保存光标状态

- 新增 headless cursor replay 扩展，分别重建活动屏幕的保存光标与当前光标，覆盖位置、颜色样式、origin、字符集和保护属性。
- 初始用例复现恢复目标错误：当前光标正确，但 ESC 8 返回第 1 列而非保存的第 4 列；修复后实际光标查询一致。
- 保护属性用例复现 saved=true 泄漏到 current=false，使可删除的 X 错误保留；改为显式输出保护状态开/关，选择性擦除结果一致。
- SGR 查询在固定 VT 上没有响应，不能作为颜色证据；改为在当前/恢复光标下实际打印字符并比较带样式的 VT 内容，避免空回复误通过。
- macOS 和 OrbStack 最终会话测试均为 21/21 通过；host 实际中文重连与 PTY 回归通过。
- 新增 `scripts/test-runtime.sh`，固定构建后执行全部当前协议/会话与四组真实进程测试；32/32 Zig 测试及各组 probe/handshake/snapshot/PTY 测试全部通过，日志 `.build/p0-native-tests.log`。
- `scripts/test-window.sh` 在新运行时再次运行真实 AppKit/Ghostty 窗口集成，1/1 通过，日志 `/tmp/aster-remote-window-tests.log`。
- `otool -L` 确认 host 可执行文件只依赖 libSystem，无 AppKit/Metal/Sparkle；固定 Ghostty 源码 LICENSE 已核实为 MIT。
- 根据实际构建和窗口链路证据勾选 P0.1、P0.3、P0.6；其余任务及 P0 总门槛保持未完成。

pending-wrap、非活动屏幕的独立保存状态、完整模式栈、图形缓存、生产操作 schema 和最终 UI 验收仍未完成；未宣称完整快照已达标。

## 行尾待换行与逐字符保护

- 复现 pending-wrap 丢失：原终端得到 `ABCDE` 后换行打印 X，恢复后覆盖 E 变成 `ABCDX`。
- cursor replay 在定位后重放该处已有字形来重建待换行位，随后只恢复属性而不再移动光标；宽字符从字形头开始，组合字符完整重放。保存光标的 pending-wrap 同时参与恢复。
- 组合用例暴露逐字符保护未序列化，导致选择性擦除丢失 ABC；formatter 现在编码保护属性变化并维护 point map，跨屏幕恢复显式清除不应继承的保护状态。
- 用例覆盖普通字符、保存/恢复光标、宽字符、组合字符和受保护宽字符；最终 macOS/OrbStack 会话测试均为 22/22 通过。
- 固定上游 `zig build test-lib-vt -Demit-lib-vt=true -Dtest-filter=formatter --summary all`：260/260 通过（124 + 136），日志 `SessionRuntime/.build/formatter-tests.log`。
- 统一 native 脚本最终 33/33 Zig 测试通过，中文重连、握手拒绝、快照故障及真实 PTY 桥接全部通过，日志 `.build/p0-native-tests.log`。

仍未完成非活动屏幕独立保存状态、完整模式栈、图形缓存、生产操作 schema 和最终 UI 验收；这些通过结果不构成 P0 总验收通过。

## 非活动屏幕保存状态与键盘模式环形栈

- 扩展 cursor replay 接受明确屏幕 ID，不通过切换 live terminal 获取状态；分别重建主/备用屏幕自己的保存光标。
- 修正 inactive alternate 重建使用 47 切换并独立恢复保存状态，避免辅助的 1049 进入动作覆盖主屏幕 save slot。
- 两个方向的重连用例均比较返回对应屏幕后执行 ESC 8 的真实光标回复；修复前返回左上角，修复后与源终端一致。
- 键盘栈用例复现只保存 top=0 导致逐层弹栈全为 0，而源终端依次为 0/3/1/0；formatter 按环形栈相对顺序重建，保留后续 push/pop 行为。
- macOS 和 OrbStack 最终会话测试均为 24/24 通过；统一 native 脚本 35/35 Zig 测试及四组真实桥接回归通过。
- 上游 formatter 定向测试再次 260/260 通过，未将基线输出的无关变化引入补丁。

其他 DEC 保存模式、图形缓存、生产操作 schema 和最终 UI 验收仍未完成；P0 总验收保持未完成。

## DEC 保存模式

- 复现当前值正确但 XTRESTORE 仍返回关闭的缺陷；新增 saved-modes 编码，先还原保存值，再由屏幕格式化还原当前值。
- 保存模式在 RIS 后、屏幕内容之前执行，避免保存/还原备用屏幕类模式时清除已重建内容。
- 明确验证默认关闭的括号粘贴/SGR 鼠标，以及默认开启的自动换行：还原前后查询均与原终端一致。
- macOS 和 OrbStack 会话测试最终 25/25 通过；统一 native 脚本 36/36 Zig 测试及中文重连、握手拒绝、快照故障、真实 PTY 桥接全部通过。
- P0.2 的 VT 适配层、输入解析、两屏读取、模式编码、尺寸变化及规范化画面编码已由对应模块/用例覆盖，标记该子任务完成。内部 Ghostty API 仍封装在 vt.zig 与固定 headless extension 中。

P0.4 完整协议 schema、P0.5 图形及综合终端语义、P0 总验收和最终 UI 验收仍未完成；没有推进到 P1。

## 图形像素缓存与安全传输基础

- 新增有界图形开关，最大 16 MiB；明确关闭 file、temporary file、shared-memory 三种加载来源。
- `copyImage` 在终端变更前复制并校验尺寸、格式、压缩状态、像素长度和分配上限；删除原图后副本仍有效。
- `Image.transmission` 将 RGB/RGBA 数据重编码为纯内联 Kitty 分块，每段不超过 4096 个 base64 字节并使用 q=2，避免恢复过程产生多余回复。
- 真实 RGBA 两像素及 64×32 数据通过 VT 解析/重放后逐字节一致；传输按 17 字节任意拆分仍可恢复，图像限额失败明确返回。
- 文件安全用例在主屏幕及惰性创建的备用屏幕先确认内联图像可用，再证明真实四字节文件不能被 f/t 图像介质读取或删除，避免“图形未启用”导致负向测试误通过。
- 最终 macOS/OrbStack 会话测试均为 30/30 通过；格式及差异检查通过。

服务默认尚未开启图形：PNG 解码、缓存枚举、placement/像素几何以及图像快照恢复仍待接入。本轮结果不代表 A03 图形或 P0.5 已通过。

## 有界真实 PNG 解码

- 复用固定 Ghostty revision 自带的 `stb_image.h`，只编译 PNG、关闭 stdio 文件入口；没有新增未锁定的外部依赖或假图像解码。
- PNG 输入与 RGBA 输出各限制 16 MiB，最大边长 16384；跟踪包含分配头的活跃工作分配总量，上限 64 MiB。解码前检查尺寸，解码后再次核对尺寸，像素通过 Ghostty 提供的 allocator 交付。
- 使用 pthread_once 安装进程级 callback，工作分配计数保持线程独立。
- 新增可复现的 RGBA、灰度、透明调色板、16-bit RGB 测试 PNG，逐字节核对真实像素；错误签名、头截断、IDAT 截断和超大尺寸拒绝后，仍可继续解码有效图片。
- 最终 macOS/OrbStack 会话测试均为 32/32，通过最新分配计数与截断用例；统一 native 检查为 43/43，加四组桥接测试通过。
- `THIRD_PARTY_NOTICES.md` 保存 Ghostty 和 stb_image 的 MIT 通知，发行时须随运行时产物携带。

PNG 能力已在 VT 图形接口启用，但服务尚未开放完整图形：缓存枚举、placement/像素几何与重连画面恢复仍未完成。P0.5 与 P0 总验收保持未完成。

## 像素几何与 placement 元数据

- 新增 Geometry 校验，只有实际报告的像素尺寸参与单元格尺寸计算；像素尺寸全为 0 表示未知，单边缺失或小于行列数量时拒绝。
- bridge 从 TIOCGWINSZ 发送 pixelWidth/pixelHeight；服务同时更新 PTY winsize 与 VT cell pixels。非法几何不会修改已有状态。
- 真实宿主 PTY 验证从 33×101 / 808×528 到 41×123 / 1230×820 的变化，远端 Python 子进程读取的行列及像素完全一致；退出清理与子进程保活保持通过。
- 新增有界 placement 元数据读取，保留图像 ID、placement ID、虚拟标记、偏移、行列范围、z 和实际像素范围。完全离屏/虚拟位置用 null 表达，不伪造 (0,0)。
- 实际图像 placement 测试验证第 3 行第 4 列、2/4 像素偏移、2×3 单元格、20×60 像素和 z=-1，超出调用方数量上限明确失败。
- 最终 macOS/OrbStack 会话测试均为 34/34，真实 PTY 端到端测试通过；新运行时的 AppKit/Ghostty 窗口集成再次 1/1 通过（约 0.535 秒）。

图像缓存枚举、完整 placement 重放、离屏/虚拟图像恢复和最终视觉验收仍未完成；服务还没有开放完整图形快照，P0.5 保持未完成。

## 可见 placement 与完整缓存 ID 枚举

- `graphics_snapshot.captureVisible` 重放可见非占位 placement，保留位置、裁剪、像素偏移、行列尺寸与 z；使用 C=1 并恢复光标，禁止图像命令移动终端光标。
- 多个 placement 共享一个 image ID 时只传输一次像素，分别保留 placement ID 与位置。
- 新增固定 headless 缓存 ID 枚举，读取明确屏幕、不切换 live 状态，排序且在分配前限制数量。包含仅上传、尚无 placement 的图像。
- 图形重放先传输全部缓存像素，再重放 placement；未显示图像也能在目标 VT 中读回。
- 可见 placement 逐字段和像素往返测试、共享图像测试、未显示缓存测试均通过；macOS/OrbStack 会话测试最终为 39/39。
- 统一 native 检查 50/50 Zig 测试及四组现有真实桥接回归通过。

当前 helper 对离屏、部分可见和虚拟 placement 明确报未编码，不静默忽略。服务图形仍未开启；全部图形状态与文本快照合并、这些 placement 类型、实际视觉验收和生产协议仍待完成，P0.5 不标记通过。

## 虚拟图形 placement

- 图形 helper 增加 U=1 虚拟 placement 编码，保留图像/placement ID、尺寸、裁剪、偏移及层级；不为虚拟 placement 伪造 viewport anchor。
- 元数据往返测试确认 virtual=true、viewport=null 且全部字段一致。
- 组合测试使用真实 Kitty Unicode 占位符及行列组合字符，前景/下划线颜色携带 image/placement 标识；文本快照和图像快照重建后，样式文本、虚拟 placement 和像素数据均与原终端一致。
- 最终 macOS/OrbStack 会话测试均为 41/41；统一 native 检查 52/52 Zig 测试与四组既有真实桥接回归通过。

离屏/部分可见 placement、全部图形与服务快照整合、实际视觉验收和生产操作协议仍未完成；P0.5 和 P0 总验收保持未完成。

## 离屏锚点与历史行重建

- 新增 full-screen/history 锚点读取，图像从部分可见滚到完全离屏后仍保留绝对历史行列，不将负 viewport row 压成 0。
- 新增有界单行导出与屏幕总行数读取，读取不移动 live terminal；越界行明确失败。
- `history_snapshot.captureContent` 按历史顺序交错重放行内容与图片锚点，使图片自然随历史滚动离开视口。稳定排序后线性消费 placement，避免 rows×placements 扫描。
- 用例同时覆盖部分可见和完全离屏，比较全部 placement 元数据、总历史行数和文本，结果与原 VT 一致。
- macOS/OrbStack 会话测试最终均为 44/44；统一 native 检查 55/55 Zig 测试和四组既有真实桥接回归通过。

当前是独立内容重建 pass，要求调用方提供同几何的新屏幕并另外恢复模式/光标；虚拟图像仍由单独 pass 处理。跨屏幕、模式及图形的完整组合、服务接入、视觉验收和生产协议仍未完成，P0.5 不标记通过。

## 混合虚拟与离屏图像

- 将 placement 编码集中为 `graphics_wire.placement`，可见/虚拟路径和历史路径共用尺寸、裁剪、偏移、层级以及 C=1/q=2 规则；固定有界编码缓冲，分配前检查输出上限。
- 历史内容 pass 先建立虚拟 placement，再按锚点重放普通图像和文本，不再因为同屏存在虚拟图像而拒绝整份内容。
- 读取源 VT 的 grapheme-cluster 模式并在内容重建前应用，移除测试对目标模式的手动预设。
- 混合测试同时包含滚到视口外的固定图像和带颜色标识的 Unicode 虚拟占位符，逐行比较样式文本、总历史行数及全部 placement 元数据。
- 最终 macOS/OrbStack 会话测试均为 45/45；统一 native 检查 56/56 Zig 测试及四组既有真实桥接回归通过。

仍需将内容 pass 与双屏幕、完整模式/光标恢复合成服务使用的完整快照，并完成图形视觉验收与生产操作协议。P0.5/P0 总验收仍未完成。

## 跨屏幕图形读取

- 图像复制、placement 几何/历史锚点和总行数现在接受明确屏幕，读取非活动屏幕时不切换 live terminal。
- 几何查询使用只读临时 projection；不执行 VT 输入、回调或资源所有权转移。
- 历史内容 pass 可显式重建指定屏幕，为后续双屏幕组合提供同一入口。
- 测试在主/备用屏幕放置相同 image ID 的红/绿不同像素；保持备用屏幕活动并调整尺寸后，重建主屏幕仍得到主屏幕的像素、placement、历史行数和逐行内容，原活动屏幕与光标不变。
- 最终 macOS/OrbStack 会话测试均为 46/46，Linux 使用包含 resize 增强用例的最终产物重跑通过；统一 native 检查 57/57 Zig 测试与四组桥接回归通过。

仍需合成最终双屏幕图形/模式/光标快照、接入服务并完成视觉验收及生产协议；P0 总验收未完成。

## 双屏幕组合、服务图形接入与 GPU 验证

- 新增 `terminal_snapshot.capture`，组合双屏幕历史、缓存、placement、当前/保存模式和光标；按源屏幕模式进入/返回，避免辅助切换清空待恢复屏幕。
- 组合用例分别以主/备用屏幕为活动屏幕，含同 ID 不同像素、主屏历史离屏图像、origin/滚动区域、鼠标/括号粘贴、键盘栈和保存光标模式；比较逐行样式、缓存像素、全部 placement、活动屏幕及状态查询。
- probe 服务启用有界内联图形；图像等待像素几何时保留缓存，hello 仍排空；快照捕获失败只关闭该客户端并记录错误，不结束后台任务。
- `graphics_service.py` 用真实 PNG 子进程输出验证：几何到达前不误发图形、分块长度和 SHA-256 正确、重放 RGBA 像素一致，重新连接保持同一进程及图像。macOS 与 OrbStack 均通过。
- 原生 AppKit/Ghostty 测试增加 PNG 绘制，读取 GPU 完成后的真实像素帧；首次和 surface 重建后均存在图像。测试固定非红色背景，防止背景导致红像素断言误通过。
- GPU 截图 `.build/remote-bridge-image.png` 已实际查看，包含红/绿 PNG 与终端文字。该证据是原生组件视觉验证，不替代所有用户级 UI 流程验收。
- 最终 macOS/OrbStack 会话测试 49/49；native 检查 60/60 Zig 测试及五组真实桥接/图形测试通过；窗口测试最终 1/1（约 0.894 秒）。

当前仍以完整快照更新，不具备文档要求的增量帧；完整操作 schema、增量切换与综合验收尚未完成。因此 P0.4、P0.5 和 P0 总验收仍不标记通过，P1–P8 仍未开始。

## 完整快照与增量切换

- 新增持续 VT 边界分类器，覆盖文本、常用绘制控制、字符集、模式、标题/链接；查询、窗口操作、敏感 OSC、图形和未知/未完整序列回到快照。UTF-8 不确定边界先重新同步。
- 分类器保留跨读取的 OSC/CSI/escape-intermediate 状态，包括忽略的 NUL；避免未完成控制字符串的后续片段泄漏为普通文字。
- 服务端发送带 baseSequence/sequence、索引及摘要的 delta 事务，桥接端完整校验后直接写入现有 VT。增量不重置本地画面。
- 桥接端独立拒绝不安全增量；缺基准时请求完整快照、丢弃失配事务及此期间输入，并清除残留前缀键。
- 独立 VT 对照覆盖快照/增量混合应用，逐步核对文字与光标；真实服务验证 full→delta→受控 full 回退→delta。
- 故障注入验证缺口重新同步、危险/过早增量拒绝、旧客户端能力拒绝，以及失效输入/前缀不在恢复后重放。
- macOS 实际 Vim 9.1 在备用屏幕启用鼠标模式并通过增量完成编辑保存（禁用用户配置、viminfo 和 swap，文件限定临时目录）。
- 最终 macOS/OrbStack 会话测试均为 55/55，Linux delta/缺口/PNG 服务测试通过；native 检查 71/71 Zig 测试与七组真实协议/桥接测试通过，窗口图像回归通过。
- 测试就绪标记改为在 READY/PNG flush 后原子发布，避免启动固定等待造成误判。
- P0.5 指定的序列、查询、图形和快照→增量切换已有分项与真实程序证据，标记该子任务完成。

P0.4 完整操作 schema 和 P0 总体审计仍未完成。增量对无法安全镜像的序列选择完整快照，不能把该策略描述为所有终端输出永远只发差分；完整帧的各安全边界仍须按最终协议及后续安全验收核对。P1–P8 未开始。

## 生产操作协议定义与请求信封

- 建立单一来源 `scripts/build-protocol.py`，生成 53 项操作的请求/结果 schema、操作目录、Swift/Zig 元数据，以及事件、画面事务和共享样例。
- 范围明确分为 bootstrap、registry、session；session 请求必须提供实例身份。结构变更要求 revision，输入/尺寸/滚动要求 lease 和 controlSequence；只读 attachment 不带写 lease。
- 补齐 request.status、upload.status、server.operation_status，处理响应丢失与跨 epoch 升级，不以重复执行猜测结果。
- Swift/Zig 请求信封共享样例覆盖全部操作、身份/范围/前置条件失败和 UInt64.max 控制/租约计数；动态 params 不承载这些精确计数。
- 使用隔离 schema-env 中锁定 jsonschema 4.25.1 及依赖；标准 Draft 2020-12 检查最终 199 组操作/事件/画面结构样例通过。
- macOS 与 OrbStack 的协议测试均为 18/18；Swift 项目定向测试 8/8（含所有 53 项操作信封），日志 `/tmp/aster-operation-tests.log`。
- 将 schema 的增量长度上限与完整帧 baseSequence 约束接回实际 bridge，超大增量拒绝用例通过。
- `protocol/contract.md` 明确 schema、信封校验与实际授权的区别，以及控制/画面通道、参数语义校验和本机机器配置边界。

尚未宣称这些操作已具备运行时 handler。强类型结果/错误/事件读写及 P0.4 完整性审计仍需收口；P0.4 和 P0 总门槛保持未完成，P1–P8 未开始。

## 结果、错误和事件编解码

- 新增 Swift/Zig 泛型结果与事件信封，嵌套 leaseEpoch、sequence、revision 均保留 UInt64 精度。
- 成功/失败响应核对在途 requestID、operation、scope 和实例目标；错误码格式与消息上限受校验。
- 事件增加独立 eventID，用于跨连接去重；校验事件种类、实例、序号回退/缺口和最小 revision。
- 共享样例覆盖最大计数、错误请求/实例、缺失修订号、错误码、事件种类、旧事件和旧修订号；两端读写均已验证。
- 标准 schema 验证 212 组通过，Zig 协议测试在 macOS/OrbStack 为 20/20，Swift 定向回归 9/9。
- P0.4 的契约、生成类型、跨语言编解码和样例已完成，标记子任务通过；这不代表各阶段运行时 handler 已实现。

## 全量测试审计（进行中）

- 当前 worktree 的标准非并行 Swift 回归与原 master 基线一样，在标签整理菜单测试附近提前以 0 退出，缺失最终汇总；日志包含真实失败，未计为通过。
- 单独标签整理测试通过。LLDB 捕获退出来自 Swift async-main drain queue；不是应用显式退出调用。
- 当前 SwiftPM 未采用实验自定义入口参数，已从实际编译 sources 核实，未将该实验当作修复。
- 独立同步 AppKit 宿主加载 bundle 并以 CInt 返回值结束测试。第一次诊断遗漏资源 flag，产生无效资源失败及后续崩溃；该次不计验收。
- 补齐 --test-bundle-path、Testing/Sparkle framework 路径后，新的全量运行已越过原退出位置且资源正常，仍在执行。运行工具句柄 42118，日志 `/tmp/aster-synchronous-verified-full.log`；继续前以实际进程/句柄核实。
- 增加可复现入口 `scripts/test-appkit-host.sh`（尚未替换标准 test.sh），宿主在 `Tests/Support/AppKitTestMain.swift`。

P0 所有实施子项已完成，但全量测试和阶段总审计尚未收口，P0 状态仍为进行中，P1–P8 未开始。

## 全量审计进展：AppKit 事件异常

- 同步宿主资源参数修正后，运行进一步到详情面板文件点击测试，但仍异常结束，不能计为全量通过。
- 独立复现证明对 applicationDefined NSEvent 读取 clickCount 会抛 NSInternalInconsistencyException。
- `PointingHandButton.sendAction` 原来未检查事件类型。现仅对左键 down/up 事件读取 clickCount，其余事件与程序化激活按单击处理；保持“文件双击打开”的既有交互。
- 新增非鼠标与单/双击测试，并通过同步宿主运行相关详情面板测试与协议回复测试，4/4 完整通过。
- 当前已启动修复后的标准 `./scripts/test.sh --no-parallel` 全量回归，工具句柄 70661，日志 `/tmp/aster-after-clickguard-full.log`。继续前必须核实该运行的实际状态，不据日志存在推定仍在运行。

P0 子任务已完成，但总体审计仍未通过；尚未进入 P1，也未提交或发布。

## 基线回归修复：Ghostty 补报提示符与真实 UI 测试

- 当前工作区仍为 `codex/remote-work`，基线 commit 为 `9e33e7b`，改动未提交。
- 上述标准宿主运行已经结束；完整同步宿主基线曾运行 1288 项测试并报告 84 个问题，日志 `/tmp/aster-guarded-sync-full.log`。不能将标准宿主的提前退出或局部通过计作全量通过。
- 静默 Agent 用例现挂载真实 Ghostty，测试 CLI 写入启动 PID 文件并保持运行；等待输入区 B 标记、进程启动及运行状态后，再验证无读屏/无 hook 的静默回退与真实 OSC hook 接管。
- 原始观察得到 `P;k=i → B → C`。锁定 Ghostty 的 zsh integration 在 PS1 被插件改写后确实生成该序列；原解析器忽略 P，导致命令时间线缺失提示符起点并一直显示非运行状态。加入固定 `P;k=i` 解析，未知属性和夹带命令仍拒绝。
- 轮询取消后增加立即退出检查，防止吞掉休眠取消后继续一次旧采样。该防护并非 P 标记问题的替代修复。
- 分屏几何测试改为查找界面实际挂载的 Ghostty；分组标题断言按现有投影区分末级组名与完整行路径。设置页确认是 WebKit 内容加原生拖动条，包含 editor 在内的十类设置。
- 命令：`./scripts/test-appkit-host.sh --no-parallel --filter 'silentAgentCLIStopsShowingProcessingBadge|shellIntegrationEventParsesStrictFTCSMarkers|ghosttyFallbackPromptTracksCommandLifecycle|ghosttyAgentLifecycleEstablishesMenuContext|splitPanesFillContentArea|sidebarOrganizerAppliesGroupingAndOrdering'`。
- 结果：退出码 0，6/6 测试通过；其中分屏参数化测试包含四种组合。日志 `.build/remote-work-evidence/ghostty-prompt-regression.log`。设置页单独验证通过；`git diff --check` 通过。
- 剩余：继续处理详情面板、主题/字体/退出恢复、输出风暴等全量失败；保留有意覆盖 SwiftTerm 兼容层的独立单元测试，只迁移实际 UI 断言。修复后重新执行完整同步宿主审计。P0 未通过总验收，P1–P8 未开始，未提交、未发布。

## 基线回归修复：详情面板、字体、实际主题帧

- 详情面板 UI 测试改为观察已挂载 Ghostty，保留终端/侧栏/详情面板身份及焦点断言。目录变化走 Ghostty 实际目录回调。
- 终端尺寸观察使用 NSView frame 通知，仅记录尺寸变化；三项展开/收起/重复切换回归完整通过。重复切换测试通过真实 PTY 输出 sentinel，验证缓冲内容不会重复，Shell 命令回显不包含完整 sentinel。
- 字号测试读取实际 Ghostty surface 的 cell 像素尺寸，确认字体增大且 surface 未被替换。主题测试采集真实 Metal BGRA 帧并验证大面积背景色，同时核对容器底色；两 Pane 均通过。
- 异常 Shell 退出测试现观察真实 Ghostty，并捕获旧代退出闭包验证恢复后迟到回调不破坏新实例。恢复行为通过，但退出码仍错误为 0，两条断言保留失败。
- 独立 PTY 复现：`/bin/sh -c 'exit 7'` 返回 7；`/usr/bin/login -q -flp <当前用户> /bin/sh -c 'exit 7'` 返回 0。Ghostty 锁定源码 Exec.zig 的 macOS 包装及 Surface.zig 的相关注释与实测一致。不能通过猜测命令文本或放宽退出码断言修复。
- 全量命令 `./scripts/test-appkit-host.sh --no-parallel` 完整退出 1，1289 tests / 35 suites，198.375 秒，16 issues。日志 `.build/remote-work-evidence/full-after-ui-migration.log`。不是测试提前退出，仍不能计为总验收通过。
- 全量剩余：异常退出 2；输出风暴旧视图引用 5；鼠标选择 4；发送到聊天 1；Prompt 队列 4。对应日志保留原始断言。
- 本轮定位及修复均在 feature worktree；`git diff --check` 通过。P0 总审计未通过，P1–P8 未开始，未提交或发布。

## 基线回归修复：输出风暴、输入与鼠标配置

- 工作区输出风暴测试现使用实际 Ghostty surface，并在其真实 PTY 中运行本次测试的 Python 输出进程。用 base64 输入驱动受控 OSC/文本输出，每批通过唯一 ACK 等待 PTY read，保留原 Session observer；独立 SwiftTerm 缓冲区单元测试保持原路径。
- 高频标题、命令生命周期、快照写入合并和 spinner 复用四项通过。spinner 只属于 Agent 是当前 Sidebar 的明确规则，测试用真实 OSC 6974 processing hook 建立 provider，不以普通进度状态假定有 Agent。
- 聊天预填与 Prompt 队列改为持有实际挂载视图，旁听真实 PTY write。聊天按现行领域文档验证目标协商后的 bracketed paste，禁止追加 Return；队列用拆分 printf 参数生成 marker，Shell 回显本身不能使测试通过，同时等待异步输入 observer 的 Return。
- 鼠标路由测试显式设置用户选择的 bypass modifiers；分别覆盖 Option 选择、手势中松开/按下修饰键、Shift 绕过和关闭绕过。原测试依赖旧的默认 Option / XTSHIFTESCAPE 行为，与当前配置合同不一致。
- `--filter 'titleStorm|commandLifecycleDoesNotRebuildWorkspace|activityBadgeRefreshReusesSpinner|readOnlyGatesUserInputWithoutBlockingProtocolResponses'`：5/5 通过，退出 0，5.390 秒，日志 `.build/remote-work-evidence/storm-readonly.log`。
- `--filter 'mouseGestureRoutingPreventsPartialReports|appModelPrefillsSelectedAgentChatWithoutSubmitting|promptQueueCardSendButtonSubmitsToCurrentCLI'`：3/3 通过，退出 0，2.320 秒，日志 `.build/remote-work-evidence/input-contracts.log`。
- 本轮完整同步宿主尝试已结束，退出码 6，没有最终 summary；异常结束前只记录退出码错误的两条 issue，末条 started 是 Read-only 测试。日志 `.build/remote-work-evidence/full-after-input-migration.log`。不能将此运行计为 1289 项全部执行完，也不能声称只有两条最终失败。
- 按末尾顺序单独运行输出风暴和 Read-only 的 5 项通过，未复现退出码 6；需要进一步定位全量宿主异常。当前没有仍在运行的测试句柄。
- 下一步：定位并修复 macOS login 包装丢失 Shell 退出码，以及全量宿主异常结束，再完成 P0 总审计。P1–P8 仍未开始；未提交、未发布。

## 基线修复：保留真实 Shell 退出状态

- 新增 vendor 配置 `aster-direct-child`，默认 false。Aster 显式 true 时 Exec 跳过 macOS login(1)，其余宿主保持既有行为；C ABI v1 布局不变。
- `TerminalSession` 将已解析的 Shell 路径与原有登录/交互参数传入 C surface；此入口只接受 Shell 文本，不支持 direct: 前缀。最终 `GhosttyConfiguration.launchCommand` 使用逐参数双引号及特殊字符转义，兼容上游 Shell 检测器，并由受控 `/bin/sh -c` 保留真实子进程退出状态。首次使用 direct: 的失败已修正，不在最终实现中。
- 原有登录 rc、Shell integration 和环境构造继续生效；不再创建额外的 login(1) 登录记账。同步 vendor README 与终端引擎领域文档。
- 在独立 `.build/ghostty-app-launch` 源码 checkout 构建新 XCFramework；仅解除本 worktree 产物 symlink 并放入新产物，主仓库产物未修改。资源 symlink 保持原同 revision 资源。生成 stamp 已更新至当前 patch digest。
- 整份 vendor patch 在干净固定 revision worktree `git apply --check --unidiff-zero` 通过；当前源码 reverse check 通过；ABI v1 与 archive member 唯一性检查通过。
- 原生 Zig：`zig build test -Dtest-filter=execCommand -Demit-macos-app=false --summary all`，59/59 build steps，73/77 tests passed，4 platform tests skipped。非本机平台跳过不是多平台运行证据。日志 `.build/remote-work-evidence/native-launch-zig.log`。
- Swift/AppKit：6/6 通过，包含真实 exit 7 异常卡片、同 Pane 重启及迟到通知隔离、静默 Agent、hook、Prompt 队列，以及特殊字符 argv/path 的真实执行。日志 `.build/remote-work-evidence/native-launch-integration.log`。
- P0 bridge：新 XCFramework 下启用真实 `ASTER_SESSION_PROBE_BINARY`，图形窗口/分离重新附加与退出/参数测试 4/4 通过，日志 `.build/remote-work-evidence/launch-bridge.log`。
- 全量调试器运行（旧产物）完成 1289 tests / 35 suites，8 issues，283.894 秒；正常由测试宿主调用 exit，未复现退出码 6。附加问题包括调试器下的延迟、SSH 输入和布局时序；退出时仍有大量终端线程，需核对测试清理。原始日志 `/tmp/aster-full-debugger.log`，不能当作正常性能验收。
- 新产物全量运行退出 137，无最终 summary；结束前没有 assertion issue。kernel 日志确认 2026-09-08 16:33:05 收到 signal 9，未提供原因；事后没有遗留测试宿主，memory_pressure 显示 45% free，不能由此反推峰值或断言是 OOM。日志 `.build/remote-work-evidence/full-native-launch.log`。
- 全量稳定性仍未解决，P0 总审计未通过，P1–P8 未开始。所有本轮测试/构建句柄已终止；未提交、未发布。

## P0 综合审计通过

### 全量执行完整性

- 新增同步 AppKit 分批驱动器；先导出完整函数清单，再依次运行有界独立宿主。逐批核对相同测试 ID、runStarted/runEnded、testEnded/testSkipped、未知错误和退出状态；零退出但缺结束记录仍失败，不自动重试。
- 清单模式不应用过滤条件，已在驱动器明确拒绝过滤/重复/自定义报告选项，并在标准入口路由为单宿主。测试默认每批 2 项；8 项 Python 审计回归通过。
- 修复测试客户端缺少 SO_NOSIGPIPE 导致拒绝连接时杀死宿主；生产服务器已有该保护。控制协议标题测试改为核对完整快照不重复、标题转换准确，允许其它合法运行态变化。
- 控制协议真实发送及 agent.prompt 用例改为实际 Ghostty 输入区就绪夹具。提示符处补齐 surface 创建时尚未取得的 Shell PID，避免后续前台 cat/Agent 被误认作 Shell；四项实际输入/等待/退出回归通过。
- 40 项批次曾有 SIGKILL 与未完成记录，未计为通过；这也是默认采用更小资源边界的依据。未宣称已证明 SIGKILL 的具体系统原因。
- 最终命令：`ASTER_TEST_BATCH_SIZE=2 ASTER_SESSION_PROBE_BINARY="$PWD/SessionRuntime/zig-out/bin/aster-session" ./scripts/test.sh --no-parallel`。
- 最终退出 0；1291 项清单，646 批，1280 项 completed，11 项 explicit skipped。核对并集严格等于原清单，无缺失、额外或重复；所有批次 exit 0，无未解决错误。8 项 Python 审计测试也通过。
- 11 项跳过是原有 10 项已被网页设置测试替代的诊断原生设置页测试，以及需显式系统 PiP 开关的用例。P0 实际图形桥接已启用且 completed，不在跳过集合。
- 完整原始事件/各批日志位于 `.build/test-runs/20260908-171032-3776/`；汇总副本 `.build/remote-work-evidence/p0-full-result.json`，控制台日志 `p0-full-run.log`。框架、输入与阶段文档检查已完成，`git diff --check` 通过。

### A01–A03 核销

| 用例 | P0 证据与结果 |
| --- | --- |
| A01 | 本文平台构建、PTY/VT、Linux 实际运行记录：macOS arm64 与 OrbStack Linux x86_64 独立 headless 产物、PTY 和服务链路通过；host 仅依赖 libSystem。其余两个平台组合仍属于 P8。 |
| A02 | 本文握手、事务、生产操作与响应章节：212 schema fixtures，macOS/OrbStack Zig 协议 20/20，Swift 共享信封及边界回归通过；53 项操作的契约不等同于实现 handler。 |
| A03 | 本文保存状态、历史/图形、增量与窗口章节：macOS/OrbStack 会话 55/55，Vim 9.1 编辑保存、查询去重、双屏/模式/图像恢复通过；新 App XCFramework 下 P0 GPU 重新附加测试及最终全量启用测试通过。 |

结论：P0.1–P0.6 与 A01–A03 的 P0 范围通过，允许进入 P1。P1–P8 仍未开始，R01–R17 尚未全部交付；现有 probe 不是用户级正式后台服务。未提交、未发布。
